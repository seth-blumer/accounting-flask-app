import React from "react";

export const styles: { [key: string]: React.CSSProperties } = {
    select: {
        padding: '10px',
        fontSize: '16px',
        borderRadius: '5px',
        border: '1px solid #ddd',
    },
};
